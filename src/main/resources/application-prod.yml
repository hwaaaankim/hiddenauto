spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/hiddenauto?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: root
    password: 'hidden3660!@'
    hikari:
      connectionTimeout: 30000
      maximumPoolSize: 10
      maxLifetime: 1800000
      poolName: HikariCP
      readOnly: false
      connectionTestQuery: SELECT 1

  h2:
    console:
      enabled: false

  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect

  servlet:
    multipart:
      maxFileSize: 5000MB
      maxRequestSize: -1

  mail:
    host: smtp.gmail.com
    port: 587
    username: 'admin@hiddenbath.co.kr'
    password: 'hfdpehjnemsxvhre'
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  upload:
    path: /home/ubuntu/hiddenauto/files/
    env: prod

server:
  servlet:
    session:
      cookie:
        secure: true

app:
  notification:
    solapi:
      # 프로젝트 내부 prod 파일은 기본 false 권장
      # 실제 운영 발송은 서버 www/config/application.yml 기준으로 관리
      enabled: false
      base-url: "https://api.solapi.com"
      api-key: "NCSGC2QXIZYWKYN5"
      api-secret: "AEKLD58N5T3RIEHWG03NN9XAJSIUB1JN"
      pf-id: "KA01PF260511070059999uAZVskW1K4k"
      default-from: "01038993660"
      webhook-secret: "hiddenauto-www-solapi-webhook-secret"
      strict: true
      allow-duplicates: false
      request-timeout-seconds: 10

logging:
  level:
    com.dev.HiddenBATHAuto.notification: INFO